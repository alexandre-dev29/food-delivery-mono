'use client';
import React from 'react';
import { Text } from '@nextui-org/react';

const OtherCompo = ({ name }: { name: string }) => {
  return <Text h1>{name}</Text>;
};

export default OtherCompo;
