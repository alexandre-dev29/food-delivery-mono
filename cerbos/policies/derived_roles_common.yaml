---
apiVersion: "api.cerbos.dev/v1"
description:
  name: common_roles
  definition:
    - name: admin
      parentRoles: [ 'user' ]
      condition:
        match:
          expr: request.principal.mainRole == "admin"
